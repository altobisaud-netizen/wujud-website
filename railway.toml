[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx serve dist/client -s -l ${PORT:-8080}"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
